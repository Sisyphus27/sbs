# SBS 训练 Web App 使用指南

本地浏览器 app。打开就是当周计划,填末组次数点提交,自动算下周。数据在 `sbs.db` 一个文件里。

---

## 一、启动

### 方式 1:双击启动(最简单)

双击项目根目录的 **`run_sbs.bat`**。

→ 自动起服务 + 开浏览器到 `http://127.0.0.1:5000`。
→ 用完关掉那个黑色命令行窗口即可。

### 方式 2:命令行

```bash
cd "D:\WorkSpace\sbs"
conda run -n sbs python -m webapp
```

### 方式 3:打包 exe(免 conda)

如果已经用 PyInstaller 打过包(见文末),双击 `dist\sbs_webapp.exe` 即可,无需 conda 环境。

> 启动后浏览器没自动开?手动访问 `http://127.0.0.1:5000`。

---

## 二、首次使用:把旧数据迁进来(只需一次)

如果你之前用过旧 CLI(`profile.yaml` + `state.yaml`),先迁进 SQLite:

```bash
cd "D:\WorkSpace\sbs"
conda run -n sbs python migrate.py
```

→ 生成 `sbs.db`(包含你所有动作 + 当前周状态)。

- 同名动作跨多天(如 Leg Curl 在 day1+day3)会作为独立实例分别保留。
- 已经有 `sbs.db` 时会拒绝覆盖。确要重来加 `--force`:`conda run -n sbs python migrate.py --force`。
- 想从原始 Excel 表重来:`conda run -n sbs python migrate.py --from-xlsx backup/00_cold_backup.xlsx`。

> 你这次已经迁过了(`sbs.db` 已生成),直接启动即可。

---

## 三、每周流程(核心)

```
1. 启动 app → 首页就是当周计划(按天列出每个动作)
2. 练完 → 回到页面,在每个动作的「末组」框填最后一组的实际次数
   → 填完一个自动保存(旁边出现 ✓),关掉浏览器也不丢
   → 每天练完填当天的就行,不必一次填完,随时可改
3. 整周都练完、数据都填好 → 点页面底部「提交并算下周」
   → 程序按三层规则更新每个动作,周数 +1,自动显示下周计划
```

**填什么**:每个动作**最后一组(末组)的实际次数**。前三组按计划走,不用填。

**规则**:
- 填的次数 ≥ 目标 → 达标,下周加重 / TM 涨。
- 超目标几次 → 多涨;差几次 → 降。
- **留空 = 没练 / 没记录 → 下周沿用原重量(不动)**。

**保存**:每填一个末组就**自动存库**(不用点提交)。所以你可以今天填 Squat、明天填 Bench……中途关浏览器、关电脑都不丢。只有「提交并算下周」才会真正推进周数。

**提交前会自动给 `sbs.db` 存一份快照**到 `backups/`,万一填错能回滚(见第六节)。

不再需要导出 JSON、传文件、开终端 —— 全在浏览器里完成。

---

## 四、管理动作(`/lifts` 页)

顶部导航点「**动作管理**」。直接在 UI 改,不再手编 YAML。

| 想做的事 | 怎么做 |
|---|---|
| 加新动作 | 顶部表单填名字 + tier + day + 参数,点「添加」 |
| 删除动作 | 该行点「删除」(会确认) |
| 改名 / 改 day / 改强度 / 改次数 | 该行输入框改值,点「保存」 |
| 换进阶层级(sbs↔t2↔t3) | 该行点「换 tier」→ 预览 → 可改起点 → 确认 |

**换 tier 会保留该动作的全部历史**,只换进阶规则;新 tier 的起点重量从历史 est1RM 推算,预览页能改后确认。

**同名动作可跨多天**:比如想在 day2 和 day4 都练 Face Pull,加两次即可,各自独立加重。

---

## 五、全局参数(`/settings` 页)

顶部导航点「**全局参数**」。改完点「保存」。

| 参数 | 含义 |
|---|---|
| rounding | 重量取整粒度(kg),默认 2.5 |
| days_per_week | 每周练几天 |
| incr | T2/T3 每周加重(kg),默认 2.5 |
| t2_reset_pct | T2 重置重量 = pct × est1RM,默认 0.7 |
| t2_fail | T2 连续几次未达标降级,默认 3 |
| t3_target | T3 末组目标次数,默认 15 |

---

## 六、备份 / 回滚

**自动快照**:每次「提交并算下周」前,自动存 `backups/sbs-w<周数>-<时间戳>.db.bak`。

**手动备份**:拷贝 `sbs.db` 到别处即可。

**回滚到某周**:
1. 关掉 app(关命令行窗口)。
2. 进 `backups/` 文件夹,挑想回到的那份 `.db.bak`。
3. 把它复制到项目根目录,重命名覆盖 `sbs.db`。
4. 重新启动 app。

---

## 七、常见问题

**Q: 启动后页面打不开?**
A: 确认命令行窗口还开着(关了就停服了)。手动访问 `http://127.0.0.1:5000`。端口被占就改(编辑 `webapp/app.py` 的 `run(port=...)`)。

**Q: 提交后某动作重量没变?**
A: 三种正常情况:① 该动作末组留空了(=没练,沿用);② T2/T3 末组没达标(不加);③ SBS 正好命中目标(0% 变化)。

**Q: 想改某动作的强度 / 分日?**
A: 「动作管理」页该行直接改,点保存,立即生效。

**Q: 填错了想撤销?**
A: 还没点「提交并算下周」→ 直接改那个框的数(改完自动覆盖保存),或清空框 = 标记没练。已经点了提交 → `backups/` 取快照覆盖 `sbs.db`(见第六节)。

**Q: 换电脑 / 重装?**
A: 拷 `sbs.db` 一个文件到新机器同位置即可。或拷整个项目文件夹。

**Q: 想彻底从头开始?**
A: 删掉 `sbs.db`,重新 `python migrate.py`(或从 UI 手动加动作)。

---

## 八、(可选)打包成 exe

免 conda,双击即跑:

```bash
conda run -n sbs python -m pip install pyinstaller
conda run -n sbs python -m PyInstaller sbs_webapp.spec --clean
```

→ 生成 `dist\sbs_webapp.exe`。把 `sbs.db` 放在 exe 同级目录,双击 exe 即用。
